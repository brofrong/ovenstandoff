{
  "name": "font-test",
  "version": "1.0.0",
  "description": "Simple website for testing Eurostile font with image",
  "main": "index.html",
  "scripts": {
    "start": "bun run --bun serve .",
    "generate": "bun run index.ts",
    "build": "bun build src/generate.ts --outdir dist --target bun"
  },
  "dependencies": {
    "@appium/opencv": "^4.0.2",
    "canvas": "^3.2.0",
    "sharp": "^0.34.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^20.0.0"
  }
}
